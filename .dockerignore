# Gradle 빌드 산출물 (컨테이너 안에서 다시 빌드하므로 불필요)
build/
.gradle/

# IDE 설정 파일
.idea/
*.iml
.vscode/

# Git 관련
.git/
.gitignore

# 환경설정/시크릿 파일 (이미지에 절대 들어가면 안 됨)
.env
*.local

# OS 관련 파일
.DS_Store

# 로그 파일
*.log

# Docker 관련 파일 자체
Dockerfile
.dockerignore
docker-compose.yml
compose.yaml